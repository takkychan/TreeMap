import React from 'react';
import '../styles/TreeMap.css';

function TreeMap(props) {
  const { data, row } = props;
  console.log('data', data);
  console.log('row', row);
  return <div className="tree-map-container">sad</div>;
}

export default TreeMap;
